function addTask() {
    const taskInput = document.getElementById('taskInput');
    const taskList = document.getElementById('taskList');
    const taskString = taskInput.value;

    //créer un élément de liste (li)
    //ajouter le contenu de l'input dans le li
    //bonus: ajouter un bouton supprimer
    //ajouter le li dans la liste
    //vider l'input
}


//fonction remove a utiliser en bonus
function removeTask(button) {
    //recuperer le li parent du bouton
    //recuperer la liste parent du li
    //supprimer le li de la liste
}
